# Open questions for next session

1. ~~**Exit rules:** hold to bear flip vs a trailing stop / profit target.~~ **Done, 2026-07-03 — baseline confirmed best.** See [options_overlay.md](options_overlay.md).
2. ~~**Entry filter:** enter on regime flip always, or wait N days / filter by VIX level?~~ **Done, 2026-07-03 — no filter justified.** See [options_overlay.md](options_overlay.md) (also corrects an earlier "high-VIX entries had the best returns" note).
3. ~~**GLD leg:** could a parallel GLD call overlay run on GLD's own signal (MA20/100)?~~ **Done, 2026-07-03 — rejected.** See [options_overlay.md](options_overlay.md): GLD's signal produces too many short whipsaw regimes for a 180-day call to survive.
4. **Real execution:** Futu HK options access, contract costs, margin treatment of long calls. Need to verify before committing capital.
5. **Kelly revisit:** once 20+ regimes have accumulated (live + historical), rerun `tools.sizing` — Kelly will become a reliable cross-check on the Calmar-derived sizes.
6. ~~**SMH options sizing:** standalone satellite position or folded into existing tooling?~~ **Done, 2026-07-03.** See [options_overlay.md](options_overlay.md) — Conservative 3% / Moderate 15% / Aggressive 20%, folds in well alongside the SPMO/QQQ overlay per `tools.portfolio_combined`. Still true: a real (not realized-vol-proxy) IV check remains the caveat before sizing real capital (`tools.options_chain_check SMH`).
7. ~~**New ticker sweep beyond the tickers already covered:** screen genuinely new asset classes for either a margin-engine diversifier or another SMH-style options-overlay candidate.~~ **Done, 2026-07-04 — nothing new beats the existing construction.** See [etf_candidates.md](etf_candidates.md): 21 new tickers screened across growth-tech sectors, metals, commodities, credit, REITs, international/EM, and crypto. SOXX/XLC/IGV/COPX confirm the drawdown-wall pattern generalizes beyond semiconductors; SOXX's options overlay works but is 0.99-correlated with SMH (redundant); EFA has genuine constraint-clean alpha but is too correlated to SPMO (0.70) to diversify; everything else (XBI, ARKK, URA, JETS, GDX, SLV, DBC, VNQ, HYG, FXI, EWZ) shows no momentum edge; IBIT has only one OOS fold, not enough data yet.
8. ~~**Sector rotation + leverage:** does layering the existing 2x-when-confirmed-strong leverage mechanism on top of the sector-rotation momentum family turn "comparable CAGR, better Sharpe/drawdown" into genuine outperformance?~~ **Done, 2026-07-06 — no, rejected (both leveraged and unleveraged).** Built as `tools/sector_rotation.py` with walk-forward OOS param selection and a permutation significance test. Walk-forward OOS underperforms SPY by ~6.5%/year unleveraged (~3.5%/year with leverage, but with meaningfully worse drawdowns), and neither version is statistically significant vs random ticker selection. See [strategy_experiments.md](strategy_experiments.md).
9. ~~**Mean-reversion for ETFs:** the macro/commodity/international ETFs rejected across this log all show near-zero alpha under MA-crossover momentum. Could a contrarian RSI or Bollinger Band strategy capture their moves instead?~~ **Done, 2026-07-06 — mostly rejected.** Built `signals/rsi_band.py` + `strategies/mean_reversion.py` (1x/0x, no leverage) + `tools/mean_reversion.py` (walk-forward OOS + circular-shift significance test) for EWJ, EEM, TLT, GDX, SLV, DBC, VNQ, HYG, FXI, EWZ. 8 of 10 show flat-to-negative OOS alpha; TLT (+4.8%) and FXI (+7.0%) are the two positive standouts but neither is statistically significant (p=0.109-0.334). Also caught and fixed a real pre-existing bug in `signals/rsi.py` (an uninterrupted-uptrend window computed RSI=0 instead of 100 — backwards). See [strategy_experiments.md](strategy_experiments.md).
10. ~~**Bear-regime put selling: real tool vs ad-hoc test.**~~ **Done, 2026-07-06.** Shipped as `tools/bear_put_overlay.py`. SPMO→QQQ: real and shipped, though MaxDD impact is worse than the original informal estimate once equity-proportional sizing is applied (-41.1% vs -35.8% baseline). SMH→SMH: tested and rejected — SMH's volatility blows through the short put. See [strategy_experiments.md](strategy_experiments.md).
11. **TLT/FXI mean-reversion re-check:** both showed positive-but-not-significant OOS alpha under RSI-band mean reversion (see #9). Worth a fresh look if either ticker resurfaces in future research (e.g. a bond-market or China-exposure thesis) — not strong enough alone to add today, but the most promising unresolved thread from this session.
12. **Diagonal spread (poor man's covered call) — untested.** Buy deep ITM LEAPS (Δ~0.85, 12–18 month expiry) as a stock replacement, sell near-term OTM calls monthly against it. LEAPS give leveraged long exposure at lower capital than shares; short calls fund theta drag. Requires monthly management. Backtest: model LEAPS purchase at regime start + monthly short-call income; compare net return vs naked long call and margin leg. Note before starting: two structurally similar ideas (bull call spread, covered calls on the margin leg) were both rejected because capping upside costs more than the premium saved on this convexity-dependent strategy — see [strategy_experiments.md](strategy_experiments.md). The short-call leg here has the same risk; the LEAPS base is the part that differs. `tools/options_backtest.py` already has the BS pricer, VIX IV proxy, regime extractor, and rolling framework to extend.
13. **Synthetic long (sell ATM put + buy ATM call, same strike/expiry) — untested.** Net premium roughly zero; stock-equivalent exposure with minimal upfront cost. Risk: the short put has uncapped downside if the regime fails (same concern as naked put selling — and see the SMH bear-put rejection in [strategy_experiments.md](strategy_experiments.md) for what that looks like on a volatile underlying). Backtest: simulate entry/exit at regime boundaries; model put assignment on losing regimes.
14. **Web UI — discussed 2026-07-03, not scoped or built.** Verdict from that discussion: worthwhile but narrowly — for *checking* things, not doing research. Priority order: (1) daily signal dashboard (signal state, days in regime, distance to flip, sizing — the most frequent action), (2) searchable browser over `charts/` PNGs and `research/` findings, (3) interactive parameter exploration (sliders recomputing sensitivity-style output). Explicitly NOT in scope: replacing the CLI for backtesting/research — keep any UI read-only/reporting; a browser-triggered arbitrary-backtest surface adds security considerations without speeding up the research loop.
15. ~~**Volatility targeting (continuous leverage scaling) — new mechanism, scoped 2026-07-09.**~~ **Done, 2026-07-09 — rejected.** Built `strategies/vol_target.py` + `tools/vol_target.py` (walk-forward OOS + a matched-average-leverage baseline + a bull-day-permutation significance test). On both live legs (SPMO, GLD), vol-scaled leverage shows ~0 Sharpe improvement over a fixed-leverage baseline carrying the identical average exposure, and isn't statistically distinguishable from a random day-to-day assignment of the same leverage values (CAGR p=0.21-0.26, Sharpe borderline p≈0.07 on both). See [strategy_experiments.md](strategy_experiments.md). Original scope, for reference: every strategy in this repo uses discrete leverage (0x/1x/2x) gated by the trend signal. Untested alternative: within a confirmed-bull regime, scale leverage continuously against trailing realized volatility instead of holding it fixed at 2x — `target_leverage = clip(target_vol / realized_vol, floor, cap)`. Mechanically different from the rejected `tools.regime_probability` (which tried to predict *direction* via logistic regression, item covered in [quant_toolbox.md](quant_toolbox.md)): vol-targeting doesn't predict anything, it risk-manages an *already-confirmed* trend (Moreira & Muir "volatility-managed portfolios" literature).
    - Not tried: SMH as a second-pass candidate (the scope's optional extension) — low priority given both live legs came back rejected.
16. **Absolute (time-series) 12-1 month momentum — untested, low priority.** A continuous momentum score (trailing 12-month return excluding the most recent month, Moskowitz/AQR-style) as an alternative to the MA-crossover threshold. Different from the already-rejected cross-sectional sector rotation (`tools.sector_rotation`, which ranks/selects *across* tickers) — this is single-ticker, still time-series. Expected to converge to the same leverage-timing conclusion as MA crossover (both are binary/near-binary trend flags per [methodology.md](methodology.md)) — worth a cheap test only as an additional confirmation, not a priority.
17. **Donchian channel breakout — untested, low priority.** N-day high/low breakout as an alternate trend signal to MA crossover. Same expectation as #16 (likely converges to the same leverage-timing conclusion) — cheap to test if wanted as one more confirmation point, not expected to change the portfolio.
